import { type Metadata } from 'next'

import { Providers } from '@/app/providers'
import { Layout } from '@/components/Layout'

import '@/styles/tailwind.css'

export const metadata: Metadata = {
  title: {
    template: '%s - GoldZulu',
    default:
      'GoldZulu - Visionary Leader, AI Enthusiast, Voice Tech Expert, Blockchain Pioneer',
  },
  description:
   'I’m GoldZulu, an innovative IT leader and AWS Community Builder based in London. With over two decades of experience, I specialize in AI, voice technology, and blockchain, pushing the boundaries of what’s possible in these cutting-edge domains.',  
   alternates: {
    types: {
      'application/rss+xml': `${process.env.NEXT_PUBLIC_SITE_URL}/feed.xml`,
    },
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="h-full antialiased" suppressHydrationWarning>
      <body className="flex h-full bg-zinc-50 dark:bg-black">
        <Providers>
          <div className="flex w-full">
            <Layout>{children}</Layout>
          </div>
        </Providers>
      </body>
    </html>
  )
}
